arrIntIndexes = [[],[],[],[],[],[],[],[],[],[]] # list of indexes where 0, 1, 2,.. occurs

** assume its filled**

imgFreq [ [28x28] , [28x28] , ... ] #each index = 0 .. 9

for curr = 0
	- get array of indexes
	for index in array
		img = extract (data[index])

		for x,y in width height
			if img[x][y] == 1
				imgFreq[curr][x][y] += 1

	- calculate based on frequencies, might need your help on this



